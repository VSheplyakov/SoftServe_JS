/**  2. Малювання у консолі
 *
 *   Створіть веб-програму, в якій користувач вводить ширину та символ.
 *   Вивести в консоль лінію заданої ширини заданим символом
 * */

let symb;
let count;

function draw(symb, count) {
  console.log(symb.repeat(count));
}
draw('5', 10)


